import pandas as pd
import msoffcrypto
import openpyxl
import os
import io
import xlrd
##
os.chdir("Directory/Name")
current_file = "Data Results.xlsx"
##
passkey = input("Please enter password: ")
decrypted = io.BytesIO()
with open(current_name, "rb") as f:
    	file = msoffcrypto.OfficeFile(f)
    	file.load_key(password=passkey)
    	file.decrypt(decrypted)
##sheet name default is 0- can change to sheet title or number in the workbook
df = pd.read_excel(decrypted, sheet_name=1, engine="openpyxl")
